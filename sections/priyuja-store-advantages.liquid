<style>
  #shopify-section-{{ section.id }} {
    --maroon: var(--priyuja-maroon);
    --maroon-deep: var(--priyuja-maroon-deep);
    --maroon-light: var(--priyuja-maroon-light);
    --gold: var(--priyuja-gold);
    --gold-light: var(--priyuja-gold-light);
    --gold-pale: var(--priyuja-gold-pale);
    --cream: var(--priyuja-cream);
    --dark: var(--priyuja-dark);
    --text-mid: var(--priyuja-text-mid);
    --text-light: var(--priyuja-text-light);
    --line: var(--priyuja-line, #E6DCC8);
    --card: var(--priyuja-card, #FFFFFF);
    --surface: var(--priyuja-surface, #FFFCF6);
    --font-heading: var(--priyuja-font-heading);
    --font-body: var(--priyuja-font-body);
    --section-max-width: {%- case section.settings.container_width -%}{%- when 'narrow' -%}840px{%- when 'wide' -%}1400px{%- when 'full' -%}100%{%- else -%}1180px{%- endcase -%};
    --section-v-pad: {%- case section.settings.section_padding -%}{%- when 'none' -%}0{%- when 'small' -%}32px{%- when 'large' -%}96px{%- else -%}54px{%- endcase -%};
    --heading-font-size: {%- case section.settings.heading_size -%}{%- when 'small' -%}24px{%- when 'large' -%}44px{%- else -%}34px{%- endcase -%};
  }
  #shopify-section-{{ section.id }} * { box-sizing: border-box; }

  .pvs-adv { font-family: var(--font-body); color: var(--dark); line-height: 1.65; background: var(--cream); padding: var(--section-v-pad) 0; border-bottom: 1px solid var(--line); }
  .pvs-adv h2, .pvs-adv h3 { font-family: var(--font-heading); color: var(--maroon); margin: 0; line-height: 1.12; }
  .pvs-adv__wrap { max-width: var(--section-max-width); margin: 0 auto; padding: 0 28px; }
  .pvs-adv__eyebrow { font-family: var(--font-body); letter-spacing: 2.5px; text-transform: uppercase; font-size: 12px; font-weight: 700; color: var(--gold); }
  .pvs-adv__grid { display: grid; grid-template-columns: repeat({{ section.settings.desktop_columns | default: 3 }}, 1fr); gap: 20px; margin-top: 24px; }
  .pvs-adv__card { background: var(--card); border: 1px solid var(--line); border-radius: 12px; padding: 22px; }
  .pvs-adv__card h3 { font-size: 20px; }
  .pvs-adv__card p { font-size: 14px; color: var(--text-mid); margin-top: 6px; }

  @media (max-width: 820px) {
    .pvs-adv__grid { grid-template-columns: repeat(2, 1fr); }
    .pvs-adv { padding: calc(var(--section-v-pad) * 0.65) 0; }
    .pvs-adv__wrap { padding: 0 18px; }
  }
</style>

<section class="pvs-adv">
  <div class="pvs-adv__wrap">
    <div class="pvs-adv__eyebrow reveal" style="text-align:center;">{{ section.settings.eyebrow | default: 'Why buy from our Varanasi store' }}</div>
    <h2 class="reveal" style="font-size:var(--heading-font-size);text-align:center;margin-top:8px;">{{ section.settings.heading | default: 'At-source advantages' }}</h2>
    {%- assign adv_cards = section.blocks | where: 'type', 'card' -%}
    {%- if adv_cards.size > 0 -%}
      <div class="pvs-adv__grid">
        {%- for block in adv_cards -%}
          <div class="pvs-adv__card reveal" {{ block.shopify_attributes }}>
            <h3>{{ block.settings.title }}</h3>
            <p>{{ block.settings.text }}</p>
          </div>
        {%- endfor -%}
      </div>
    {%- endif -%}
  </div>
</section>

{%- render 'priyuja-reveal', section_id: section.id -%}

{% schema %}
{
  "name": "Priyuja - Advantages",
  "tag": "section",
  "class": "priyuja-section",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Why buy from our Varanasi store"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "At-source advantages"
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "container_width",
      "label": "Container width",
      "options": [
        { "value": "narrow", "label": "Narrow (840px)" },
        { "value": "default", "label": "Default (1180px)" },
        { "value": "wide", "label": "Wide (1400px)" },
        { "value": "full", "label": "Full width (100%)" }
      ],
      "default": "default"
    },
    {
      "type": "select",
      "id": "section_padding",
      "label": "Section padding",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "small", "label": "Small" },
        { "value": "medium", "label": "Medium" },
        { "value": "large", "label": "Large" }
      ],
      "default": "medium"
    },
    {
      "type": "range",
      "id": "desktop_columns",
      "label": "Desktop columns",
      "min": 2,
      "max": 4,
      "step": 1,
      "default": 3
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "Heading size",
      "options": [
        { "value": "small", "label": "Small" },
        { "value": "medium", "label": "Medium" },
        { "value": "large", "label": "Large" }
      ],
      "default": "medium"
    }
  ],
  "blocks": [
    {
      "type": "card",
      "name": "Advantage Card",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Card Title",
          "default": "Advantage"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Card Text",
          "default": "Advantage description."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Priyuja - Advantages"
    }
  ]
}
{% endschema %}
